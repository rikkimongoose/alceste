<UserControl x:Class="Alceste.LocalApp.AudioStream.AudioStreamChannelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid Name="gridMediaView">
            <Image Name="imgMediaView" Source="{Binding ImageSource}" MouseUp="ImgMediaView_OnMouseUp" />
            <Rectangle Name="rectStatus" Width="2" HorizontalAlignment="Left" Margin="{Binding Margin}"  Fill="{Binding LineBrush}" />
        </Grid>
    </Grid>
</UserControl>
